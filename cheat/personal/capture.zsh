
# Usage:
  capture.zsh bufferstring

# Examples:

  capture.zsh 'vim -'
  capture.zsh 'vim --'
  capture.zsh 'vim --r'
  capture.zsh 'echo *('
  capture.zsh 'scp hostname:'

NOTE: from https://github.com/Valodim/zsh-capture-completion
